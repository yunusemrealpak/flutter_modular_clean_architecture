/// Theme switch variants
enum AppThemeSwitchVariant {
  /// Simple toggle switch (Light ↔ Dark)
  toggle,

  /// Icon button (cycles through modes)
  iconButton,

  /// Segmented control (Light / System / Dark)
  segmented,
}
